{
  "name": "jonna-aubergine",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx -y servor . index.html 3000",
    "build": "mkdir -p dist && cp -r src index.html manifest.json icon.svg recipes.json sw.js dist/"
  }
}
